from engine import run_pipeline
if __name__=='__main__':
 import argparse
 ap=argparse.ArgumentParser(); ap.add_argument('--season', required=True); ap.add_argument('--date', default='')
 a=ap.parse_args(); run_pipeline(season=a.season, date=a.date)
